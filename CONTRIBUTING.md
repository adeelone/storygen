# Contributing

Create a branch from `main`, keep commits conventional, and keep provider behavior behind its interface. Run `make lint`, `make typecheck`, `make test`, and `make eval` before submitting a pull request. No live provider requests belong in CI tests; record sanitized fixtures or use the mock provider.
